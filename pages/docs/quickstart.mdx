import {Tabs} from 'nextra/components'

## Quickstart

Kickstart your journey with our quickstart guide, where simplicity meets style. Follow these steps, and you'll be up and running faster than you can say "cold brew."

## Installation

<Tabs items={['Javascript', 'Python']}>

  <Tabs.Tab>
    ```bash
    npm install cool-library --save
    ```
  </Tabs.Tab>

  <Tabs.Tab>
    ```bash
    pip install cool-library
    ```
  </Tabs.Tab>

</Tabs>

## Initialization

<Tabs items={['Javascript', 'Python']}>

  <Tabs.Tab>
    ```js filename="main.js" showLineNumbers
    // Import and initialize the library
    import CoolLibrary from 'cool-library';

    const app = new CoolLibrary({
      flavor: 'espresso',
      vibe: 'chill'
    });
    ```
  </Tabs.Tab>

  <Tabs.Tab>
    ```python filename="main.py" showLineNumbers
    # Import and initialize the library
    from cool_library import CoolLibrary

    app = CoolLibrary(flavor='espresso', vibe='chill')
    ```
  </Tabs.Tab>

</Tabs>

## Configuration

<Tabs items={['Javascript', 'Python']}>

  <Tabs.Tab>
    ```javascript filename="config.js" showLineNumbers
    // Set up your configuration
    app.configure({
      apiKey: 'your-api-key',
      debugMode: true,
      theme: 'minimalist'
    });
    ```
  </Tabs.Tab>

  <Tabs.Tab>
    ```python filename="config.py" showLineNumbers
    # Set up your configuration
    app.configure(
        api_key='your-api-key',
        debug_mode=True,
        theme='minimalist'
    )
    ```
  </Tabs.Tab>

</Tabs>

## Basic Usage

<Tabs items={['Javascript', 'Python']}>

  <Tabs.Tab>
    ```javascript filename="src/app.js" showLineNumbers
    // Basic usage example
    app.start(() => {
      console.log('App is running with a vintage touch!');
    });
    ```
  </Tabs.Tab>

  <Tabs.Tab>
    ```python filename="main.py" showLineNumbers
    # Basic usage example
    def callback():
        print('App is running with a vintage touch!')

    app.start(callback)
    ```
  </Tabs.Tab>

</Tabs>

## Customization

<Tabs items={['Javascript', 'Python']}>

  <Tabs.Tab>
    
    ```javascript filename="src/app.js" showLineNumbers
    // Customize the experience
    app.customize({
      font: 'Roboto Mono',
      colorScheme: 'dark',
      animations:

    ```
  </Tabs.Tab>

  <Tabs.Tab>
    ```python filename="main.py" showLineNumbers
    # Customize the experience
    app.customize(
        font='Roboto Mono',
        color_scheme='dark',
        animations={
            'enable': True,
            'speed': 'fast',
            'transitions': ['fade', 'slide']
        }
    )
    ```
  </Tabs.Tab>

</Tabs>